// Stub for Node's `stream` module — papaparse only needs this for its streaming API
// which we don't use. React Native has no stream support.
module.exports = { Duplex: function () {}, Writable: function () {}, Readable: function () {} };
